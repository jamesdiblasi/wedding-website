import { Logger } from "backfill-logger";
import { RepoInfo } from "./repoInfo";
export declare type PackageHashInfo = {
    name: string;
    filesHash: string;
    dependenciesHash: string;
    internalDependencies: string[];
};
export declare function generateHashOfInternalPackages(internalPackages: PackageHashInfo[]): string;
export declare function getPackageHash(packageRoot: string, repoInfo: RepoInfo, logger: Logger): Promise<PackageHashInfo>;
//# sourceMappingURL=hashOfPackage.d.ts.map